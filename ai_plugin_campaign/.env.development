# 本地开发同样调用真实学生接口，确保服务端确认成功后才写入本地学生信息。
# 只有纯 UI 联调时才临时改为 mock，联调完成后请恢复 production。
VITE_AI_EDU_API_MODE=production

# cloud.zbrowser.cn 的线上 CORS 仅允许正式域名，本地通过 Vite 代理联调真实作品接口。
VITE_AI_PRODUCTS_URL=/api/ai-products
